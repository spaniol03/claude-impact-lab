# Runtime do backend (API). Faixas compatíveis; versões exatas em requirements.lock.txt
# (gerado com `pip freeze` após instalação bem-sucedida).
fastapi>=0.115,<1.0
uvicorn[standard]>=0.30,<1.0
pydantic>=2.9,<3.0
pydantic-settings>=2.5,<3.0
SQLAlchemy>=2.0,<3.0
